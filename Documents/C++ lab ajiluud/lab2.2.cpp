#include <stdio.h>
#include <stdlib.h>

int main() {
    char ovog[50], ner[50], mergejil[50];

    printf("ovog oruul: ");
    scanf("%s", ovog);
    printf("ner oruul: ");
    scanf("%s", ner);
    printf("mergejil oruul: ");
    scanf("%s", mergejil);

    printf("ovog: %s\n", ovog);
    printf("ner: %s\n", ner);
    printf("mergejil: %s\n", mergejil);
    return 0;
}
